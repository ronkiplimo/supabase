---
title: State Badge
description: Segmented operational status badges for delivery outcomes, enabled or disabled state, and unknown state.
fragment: true
---

<ComponentPreview name="state-badge-demo" peekCode />

## Usage

```tsx
import { StateBadge } from 'ui-patterns/StateBadge'
```

```tsx
<StateBadge state="success" />
```

## API Reference

| Prop        | Type                                                                                       | Default | Description                                                            |
| ----------- | ------------------------------------------------------------------------------------------ | ------- | ---------------------------------------------------------------------- |
| `state`     | `'success' \| 'failure' \| 'pending' \| 'skipped' \| 'enabled' \| 'disabled' \| 'unknown'` | —       | Semantic state. Determines both the icon and the badge tone.           |
| `children`  | `React.ReactNode`                                                                          | —       | Optional label override. Keeps the semantic icon and colour treatment. |
| `className` | `string`                                                                                   | —       | Additional classes applied to the root wrapper.                        |

## Guidance

Use `StateBadge` for short operational states where icon plus label improves scanability, such as delivery results, enabled and disabled status, or operational uncertainty.

Keep the text to one or two words. For broader product categories or generic contextual metadata, keep using the `Badge` atom instead.
